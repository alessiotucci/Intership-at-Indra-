// See https://aka.ms/new-console-template for more information
using NetflixClone.Data;
using NetflixClone.Models;

Console.WriteLine("NETFLIX CLONE DB ");
/*
using (var context = new NetflixCloneDbContext())
{
    var u_test = new Utente();
    u_test.Name = "test";
    u_test.Data_Subscription = DateTime.Now;
    u_test.Iban = "abcdefghilmn12345";

    context.Utenti.Add(u_test);
    context.SaveChanges();
}
*/